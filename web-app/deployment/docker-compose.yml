services:
  web-app:
    container_name: web-public-website
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3034:3034"
    env_file:
      - .env
    command: npm start
    restart: always
